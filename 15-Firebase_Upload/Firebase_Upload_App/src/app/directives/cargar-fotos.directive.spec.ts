import { CargarFotosDirective } from './cargar-fotos.directive';

describe('CargarFotosDirective', () => {
  it('should create an instance', () => {
    const directive = new CargarFotosDirective();
    expect(directive).toBeTruthy();
  });
});
